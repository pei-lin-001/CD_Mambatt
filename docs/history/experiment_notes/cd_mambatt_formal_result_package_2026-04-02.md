# CD-MambAtt Formal Result Package (2026-04-02)

## 1. Purpose

This note freezes the current **reproducible experimental package** after the
completion of the fair `15-shot` comparison against the reproduced FOMLN
baseline.

It answers three questions:

1. What has been fully reproduced and logged?
2. What is the strongest current result claim we can make?
3. What should be the next experimental priority?

---

## 2. What is now completed

### 2.1 Original target-paper supervised baseline

- single-domain supervised MambAtt reproduction completed
- main alignment notes already recorded in:
  - `/home/shelterpl/cd_mambatt/docs/paper_alignment_supervised.md`
  - `/home/shelterpl/cd_mambatt/docs/supervised_reproduction_tiers.md`

### 2.2 CD-MambAtt cross-domain method

- `5-shot` multi-task package completed on the main overlapping transfer pairs
- best working configuration:
  - `lambda_mmd = 0.1`
  - `lambda_source_stage = 1.0`
  - `lambda_pseudo = 0.5`
  - `lambda_monotonic = 0.05`
  - `lambda_contrastive = 0.0`
  - pseudo quantile schedule = `0.5 -> 0.9`

### 2.3 Published-baseline reproduction: FOMLN

- formal reproduced FOMLN baseline completed
- matched `15-shot`, `5-seed` results available on:
  - `FD001 -> FD003`
  - `FD003 -> FD001`
  - `FD002 -> FD004`
  - `FD001 -> FD004`

### 2.4 Fair head-to-head package

- CD-MambAtt also rerun under matched `15-shot`
- fair `CD-MambAtt 15-shot vs FOMLN 15-shot` table is now complete on the same
  4 overlapping transfer pairs

---

## 3. Current strongest formal claim

> Under the matched `15-shot` target-shot budget, **CD-MambAtt beats the
> reproduced FOMLN baseline on all 4 overlapping C-MAPSS transfer pairs**.

### Fair comparison table

| Task | CD-MambAtt 15-shot | FOMLN 15-shot | Δ (CD - FOMLN) |
|---|---:|---:|---:|
| `FD001 → FD003` | **20.1384** | 25.4748 | -5.3364 |
| `FD003 → FD001` | **19.5788** | 20.1261 | -0.5474 |
| `FD002 → FD004` | **23.2879** | 26.0206 | -2.7327 |
| `FD001 → FD004` | **23.9469** | 25.0808 | -1.1340 |

Interpretation:

- negative `Δ` means CD-MambAtt is better
- current result is **4 / 4 wins**

---

## 4. Data-efficiency snapshot

Although not a fair protocol match, it is still useful to compare our original
`5-shot` CD-MambAtt line with the reproduced `15-shot` FOMLN line.

| Task | CD 5-shot | CD 15-shot | FOMLN 15-shot |
|---|---:|---:|---:|
| `FD001 → FD003` | **22.0342** | **20.1384** | 25.4748 |
| `FD003 → FD001` | **19.8137** | **19.5788** | 20.1261 |
| `FD002 → FD004` | **22.1776** | **23.2879** | 26.0206 |
| `FD001 → FD004` | **24.3449** | **23.9469** | 25.0808 |

Current interpretation:

- CD-MambAtt remains strong even under the stricter `5-shot` regime
- giving CD-MambAtt `15-shot` mostly improves or stabilizes the result further
- the strongest evidence is still the **matched fair `15-shot` comparison**

---

## 5. Reproducible artifacts

### Main logs / reports

- experiment log:
  - `/home/shelterpl/cd_mambatt/docs/cross_domain_experiment_log.md`
- fair comparison note:
  - `/home/shelterpl/cd_mambatt/docs/cd_mambatt_vs_fomln_formal_comparison_2026-04-02.md`
- FOMLN progress note:
  - `/home/shelterpl/cd_mambatt/docs/fomln_baseline_progress_2026-04-01.md`

### Auto-generated tables

Generated by:

- `/home/shelterpl/cd_mambatt/scripts/aggregate_formal_result_tables.py`

Outputs:

- `/home/shelterpl/cd_mambatt/docs/generated/fair_comparison_cd_mambatt15_vs_fomln15.csv`
- `/home/shelterpl/cd_mambatt/docs/generated/data_efficiency_snapshot_cd5_cd15_fomln15.csv`
- `/home/shelterpl/cd_mambatt/docs/generated/formal_result_snapshot.json`
- `/home/shelterpl/cd_mambatt/docs/generated/formal_result_tables.md`

---

## 6. What is still missing before a stronger submission package

The project is now beyond “idea validation”, but still short of a final
submission-grade package.

The biggest remaining gaps are:

1. **Second published baseline**
   - current best next target: **MetaDFKN**
   - but only after protocol cleaning from the local PDF

2. **Second dataset**
   - recommended next dataset: **XJTU-SY**

3. **Final multi-task ablation package**
   - remove `MMD`
   - remove `pseudo`
   - remove `monotonic`
   - ideally report on more than one transfer pair

---

## 7. Recommended next step

### Immediate next priority

> Start a **MetaDFKN protocol-cleaning note** from the local paper, then decide
> whether a true reproduction is feasible and fair.

Reason:

- FOMLN has already been turned into a real reproduced baseline
- MetaDFKN is the next most threatening reported paper baseline
- but its protocol still contains ambiguity and should not be reproduced
  blindly

### Practical execution order

1. audit the local MetaDFKN PDF into a clean protocol note
2. decide whether it is reproducible under a fair target-domain setting
3. if yes, implement a minimal MetaDFKN baseline
4. after that, start the second dataset branch

---

## 8. Bottom-line status

As of `2026-04-02`, the project now has:

- a supervised target-paper baseline
- a working cross-domain CD-MambAtt package
- a formal reproduced FOMLN baseline
- a completed fair `15-shot vs 15-shot` comparison

This is the first point where we can say the project has a **real, reproducible
cross-domain baseline package**, not just exploratory runs.
